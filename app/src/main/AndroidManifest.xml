<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.sscholz.converse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/converse_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/converse_logo"
        android:supportsRtl="true"
        android:theme="@style/myMainTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"/>
        <activity
            android:name=".HelpActivity"
            android:label="@string/title_activity_help"/>
        <activity
            android:name=".MyEditFileActivity"
            android:label="@string/title_activity_my_edit_file"/>

        <activity
            android:name=".GolActivity"
            android:label="Game Of Life"/>

    </application>

</manifest>